func main() {
    let line x: int = 4;
    if x == 4 {
        print 'x is 4';
    }
    x = 5;
    for let global i: int in range(x) {
        print global i;
    }
    while true {
        x = x + 1;
        print x;
        if x == 100 break;
        else if x == 99 print 'almost there!';
    }
    let local i: float = 2.;
    let a: float, b: int = (local i, 2);
    let ab: (float, int) = (a, b);
}
